# Efficient sampling comparison

Run a single new balanced50_v1 arm from the verified epoch100 quarter-rate full checkpoint through epoch110. Reuse the completed control_seg1e-4 arm from paired_loss_20260918_141555. Its reference checkpoint SHA256, optimizer/LR/loss, seed, batch size and202attempts/epoch were checked. No control rerun; comparison is historical/nonconcurrent, not guaranteed bitwise paired hardware execution. Sampling intentionally changes the data/RNG sequence.

Policy: with probability0.5 use the existing shuffled scene and original crop/flip; otherwise uniformly choose one of all22 foreground classes, uniformly choose a training scene with accessible pixels, then uniformly choose a legal col10..245 with at least half the maximum attainable target pixels for that scene/class. Fixed vertical band170:426 and256x256 size retained. Scene repeats/omissions are intentional. No validation/test labels guide the sampler; all22 classes are treated equally. PNG labels are indexed once in CPU memory (~53MB), avoiding repeated label scans to choose a crop. No image cube normalization/augmentation changes. No AMP, architecture or metric changes.

Loss remains lambda_seg1e-4, lambda_rec1; retain epoch100 Adam moments, scaler and quarter-rate500-epoch scheduler. Only sampling policy changes. Resume guards enforce policy consistency; missing legacy field means baseline. Per-epoch logs record actual class pixels/crops, targeted classes and scene draws. These include attempted batches, even if AMP skips an update. There is no claim that class balance fixes confusion or constitutes journal novelty.

Success assessment: compare full trajectories, final and last5 mean mIoU, per-class metrics and reconstruction against historical control; inspect banana/avocado and any regression elsewhere. Audit whether realized exposure changes as intended. Preserve prior60.82% validation-selected candidate separately. New branch best-selection starts from epoch100 weights, as in the historical control. Do not cherry-pick the peak alone or tune against test. One exploratory seed cannot establish statistical superiority.

Validation: CPU synthetic checks verify22-class support, image/label alignment after crop+flip, deterministic replay, targeted minimum coverage, unchanged original branch and no RNG consumption while building the index. Existing loss/resume tests pass. Preparation from the real epoch100 archive preserves model/optimizer/scheduler/scaler/RNG and rejects a sampling-policy mismatch. Notebook cells compile; GPU training pending.
