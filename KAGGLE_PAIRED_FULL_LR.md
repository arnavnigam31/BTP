# Matched full-dataset learning-rate continuation

Starting evidence: saved best weights reproduce44.0896% foreground validation mIoU, while epoch50 weights reproduce22.7741%. Two identical best-weight evaluations yield byte-identical metrics and confusion matrices. This supports a controlled training experiment, not a claim that LR is the known cause.

Both arms start from the same full epoch50 state, training epochs51-60 on202 training scenes with25 validation scenes per epoch. Control retains the500-epoch cosine schedule. Quarter arm scales the current LR, base LR and remaining schedule floor by0.25 (approximately9.7559e-5 starting LR versus3.90236e-4 control; floor2.5e-7 versus1e-6). Adam moments/step, model, scaler, RNG, crop rules, loss weights and data remain identical at initialization. This differs from resetting Adam or starting from the best weight-only file. Subsequent AMP skips, if any, are reported; GPU arithmetic is not promised bitwise deterministic.

Original checkpoints are read-only. Derived initial states are explicitly recorded as an experiment branch. Each arm selects best weights among its shared epoch50 initialization and epochs51-60; historical epoch49 best is not counted as an arm's result. Keep the original epoch50 archive containing epoch49 best separately. The launcher uses existing train.py and resume validation without changing baseline behavior. It refuses an existing output directory. The original baseline arm's optimizer/scheduler remain unchanged; best-selection bookkeeping is scoped to the experiment.

Compare final metrics, complete trajectories, last-five-epoch means/standard deviations, per-class recall/IoU and reconstruction; report both quality tradeoffs and actual updates. Recent-window standard deviation is descriptive, not a confidence interval. One paired seed is exploratory, not proof of superiority. Keep the test split untouched. If stopped mid-experiment, retain the partial archive and do not rerun into the existing directory blindly.

Validation: CPU fixture confirms no reference-state mutation, equal model/Adam moments/scaler/RNG, original control schedule and persistent0.25 LR ratio over10 resumed scheduler steps. The real epoch50 archive passes preparation, dataset/mask identity, protocol preflight and paired-state checks without GPU training. Notebook cells compile. First full GPU run remains pending.
