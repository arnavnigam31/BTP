# Training-scene diagnostic after epoch96 validation error audit

Eight class/scene pairs: two training scenes each for real lemon, fake avocado, real banana and fake unknown. Rank by maximum target-class pixels attainable in the unchanged legal row170:426, col10..245 crop. Resolve crop ties closest to128 then smaller col, scene ties lexicographically. This deliberately favors class-rich training examples, chosen using labels without model output. It is not an unbiased training metric. A scene may be selected for more than one class.

Use the hash-verified epoch96 best weights in eval mode, float32, no flips or optimization. For each selection report full-frame inference, its predictions scored on the selected ROI (no second forward pass), fresh crop inference with the corresponding mask location, and fresh crop inference with a deterministic independent training-style mask crop. All three ROI modes score identical ground-truth pixels. The registered cropped measurement changes boundary conditions/context relative to full-scene acquisition, so a difference cannot be attributed solely to network input size. Independent mask offsets are recorded and match production crop_mask slicing; image and mask crops are sampled independently during baseline training.

Save class-ID PNGs, per-scene confusion matrices, previews and reconstruction metrics without HSI cubes. Compare target recall/IoU and confusion destinations. Strong training and weak validation suggests a generalization gap on these selections; weak training also suggests an optimization/representation problem. Neither outcome alone identifies a causal mechanism or authorizes relabeling. Test pixels remain untouched; metadata protocol preflight covers memberships.

Validation: CPU exact comparison of SSR slicing against production crop_mask passes. Training-only selection/preflight runs on real local data and records all selections. Notebook cells compile. The new inference orchestration still requires its first Kaggle GPU run; no successful GPU diagnostic is claimed.
